# 01: Publish spec — type-safe shorthands + controllable framework errors

**What to build:** Spec file synthesized from conversation + ADRs/glossary, covering ApiMethodHelper overload fix and 400/401/404/500 auto-doc policy, published to tracker with `ready-for-agent`.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Spec uses glossary vocabulary (ApiBuilder, ApiMethodHelper, ReqFor, StoredRoute, etc.) and respects ADRs 007/009/010
- [ ] Seams sketched (S1 typecheck, S2 app.request) and checked with user
- [ ] File published to tracker with `ready-for-agent` label

> Local spec: `.scratch/to-spec/SPEC-method-shorthands-and-framework-errors.md`
