pre-commit:
  commands:
    lint:
      run: nubx -y @biomejs/biome check --write --unsafe src/ examples/
      stage_fixed: true
    typecheck:
      run: nub run typecheck

pre-push:
  commands:
    tests:
      run: nub run check:all
